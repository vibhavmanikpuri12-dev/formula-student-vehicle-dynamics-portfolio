# Simulation Files

The full ANSYS simulation result archive is intentionally not included in the browser-safe core upload package.

Reason: GitHub browser uploads can become unstable when uploading large binary folders. Upload this later through Git command line or attach selected result images in `assets/simulation-renders/`.
